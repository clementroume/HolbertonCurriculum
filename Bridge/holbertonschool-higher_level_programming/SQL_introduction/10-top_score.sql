-- This script retrieves all records from the `second_table` in the `hbtn_0c_0` database and orders them by the `score` column in descending order.
SELECT score, name FROM second_table
ORDER BY score DESC;
