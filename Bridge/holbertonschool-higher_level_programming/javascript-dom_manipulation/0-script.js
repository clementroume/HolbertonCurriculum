// Sélectionne le premier élément <header> dans le document HTML
const header = document.querySelector('header');

// Change la couleur du texte de l'élément <header> en rouge (#FF0000)
header.style.color = '#FF0000';
